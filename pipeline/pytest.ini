[pytest]
testpaths = tests
python_files = test_*.py
# src/ lives beside tests/, so pipeline/ has to be importable. `python -m pytest`
# adds the cwd to sys.path but the bare `pytest` console script does not.
pythonpath = .
addopts = -q --strict-markers
